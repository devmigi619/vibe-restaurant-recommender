# 로컬 Docker 실행
# Frontend: http://localhost:3000 (nginx 컨테이너)
# Backend:  http://localhost:8000 (백엔드 컨테이너)
# 실행: docker compose --env-file .env.local.docker up --build

VITE_API_URL=http://localhost:8000
CORS_ORIGINS=http://localhost:3000
